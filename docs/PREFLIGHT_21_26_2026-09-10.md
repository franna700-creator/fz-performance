# FZ Performance v0.7 RC7 — PREPARE NOW 21–26

Status: PRE-DEPLOYMENT CANDIDATE PREPARATION.

## 21 — complete repository candidate
RC7 is assembled as a deterministic overlay over RC1 commit `bcb8b9299ff309652470962288f611a4ff6e09b8` / tree `f1f4ebc1a654e4459db1af5064312108e5ed3077`. The immutable Git tree inherits every unchanged RC1 file and replaces/adds only the manifest-listed RC7 files. Source-only smoke checks pass. Full inherited repository build is required on the exact frozen candidate SHA before Vercel deployment.

## 22 — immutable release SHA
Freeze only after 23–26 are complete. `main` must not move during preparation. The exact frozen candidate SHA is the sole eligible input to full CI and later Vercel deployment.

## 23 — CI/workflow verification
`quality.yml` runs `npm run build`, syntax checks runtime/API/library files, exercises legacy and RC7 smoke suites, and packages a validated release artifact. RC7 adds explicit syntax/execution coverage for event-format, event-intelligence and contract/dependency-closure checks.

## 24 — migration rehearsal
See `MIGRATION_005_REHEARSAL.md`. Migration 005 passed two isolated-branch rehearsals; final DDL is idempotent, constraints/indexes were verified, invalid strategic weight was rejected, revision history protected its objective, and production still contains zero of the new tables.

## 25 — canonical data-contract registry v1
`lib/data-contract-registry.js` now owns 20 current decision-driving concepts. Each declares owner layer, canonical source/derivation, definition, refresh triggers, freshness, missing-data policy, fallback, provenance, primary UI surface and consumers.

## 26 — dependency/invalidation matrix
`lib/runtime-dependency-graph.js` defines transitive invalidation from source evidence to canonical truth, derivations, adaptive context and UI surfaces. `contract-dependency-closure-smoke.mjs` fails if registry triggers/consumers and graph closure diverge.

## Release boundary
No production database change. No Vercel deployment. No Tranche 4.2 recommendation recomputation. No Tranche 4.3 athlete override UI.
