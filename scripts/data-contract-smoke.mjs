import { FZ_DATA_CONTRACTS, validateDataContractRegistry, contractById } from '../lib/data-contract-registry.js';
const result=validateDataContractRegistry();if(!result.ok)throw new Error(result.errors.join('; '));if(FZ_DATA_CONTRACTS.length!==24)throw new Error(`Expected 24 canonical contracts, found ${FZ_DATA_CONTRACTS.length}`);for(const id of ['wellness.current','training.evidence','athlete.memory','trends.ncl','event.format','event.demand_taxonomy','objective.graph','capability.evidence','capability.priority','measurement.hierarchy','materiality.current','adaptive.context','recommendation.shadow','recommendation.current','recommendation.explanation'])if(!contractById(id))throw new Error(`Missing contract ${id}`);if(contractById('trends.ncl').missingPolicy!=='PENDING_DETAIL_NEVER_ZERO')throw new Error('NCL missing-data rule regressed');if(contractById('event.format').fallbackPolicy!=='KEEP_EVENT_SCHEDULED_BUT_EXCLUDE_FROM_TRANSFER')throw new Error('Event knowledge gate regressed');if(!contractById('recommendation.shadow').fallbackPolicy.includes('NO_WRITE_TO_RECOMMENDATION_CURRENT'))throw new Error('4.2 shadow isolation contract regressed');if(contractById('recommendation.current').refreshTriggers.some(x=>x==='adaptive.context'||x==='materiality.current'))throw new Error('Shadow/adaptive context may not directly refresh active recommendation.current');console.log('PASS v0.8 RC1 data contract registry: 24 canonical concepts including isolated recommendation.shadow');
