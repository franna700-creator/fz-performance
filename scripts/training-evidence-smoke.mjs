import assert from 'node:assert/strict';import { deriveNcl, fillEvidenceGaps, mergeEvidenceRows } from '../lib/training-evidence.js';const older={ingested_at:'2026-09-09T15:42:52Z',payload:{detailLevel:'activity-detail',summary:{durationTotal:1523,heartrate:147,intensityDistribution:{heartrate:{0:557,1:891,2:75}},zonesDistribution:{heartrate:[557,891,75]}}}},newerSparse={ingested_at:'2026-09-10T04:33:04Z',payload:{detailLevel:'activity-detail',summary:{durationTotal:1523,heartrate:148,intensityDistribution:{heartrate:null},zonesDistribution:{heartrate:null}}}};const merged=mergeEvidenceRows([older,newerSparse]);assert.equal(merged.payload.summary.heartrate,148,'newer present values must win');assert.deepEqual(merged.payload.summary.zonesDistribution.heartrate,[557,891,75],'older richer evidence must fill a newer gap');assert.equal(merged.enrichedFromHistory,true);assert.ok(Math.abs(deriveNcl(merged.payload)-43.9833)<0.0001,'AET NCL must survive sparse refreshes');assert.equal(fillEvidenceGaps(0,7),0,'zero is observed data, not missing');assert.equal(fillEvidenceGaps(false,true),false,'false is observed data, not missing');console.log('PASS monotonic best-available training evidence');
