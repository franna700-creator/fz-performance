import fs from 'node:fs';
const html=fs.readFileSync('dist/index.html','utf8'),ui=fs.readFileSync('dist/assets/system-intelligence.js','utf8'),systemApi=fs.readFileSync('api/system/status.js','utf8'),materiality=fs.readFileSync('lib/materiality-engine.js','utf8'),shadow=fs.readFileSync('lib/recommendation-engine.js','utf8');
const checks=[['observability asset wired before clean app',html.includes('/assets/system-intelligence.js')&&html.indexOf('/assets/system-intelligence.js')<html.indexOf('/assets/app-clean.js')],['observability piggybacks on canonical runtime reads',["'/api/system/status'","'/api/training/memory'","'/api/trends/current'"].every(x=>ui.includes(x))&&ui.includes('captureCanonicalResponse')],['dynamic integrity surface present',ui.includes('Dynamic Runtime Integrity')&&ui.includes('unlinkedSessionFeedback')&&ui.includes('loadMissingDates')],['4.2 shadow surface present',ui.includes('Recommendation Shadow')&&ui.includes('TODAY activation')&&ui.includes('Shadow output cannot write recommendation.current or alter TODAY.')],['observability writes cannot self-trigger indefinitely',ui.includes('renderScheduled')&&ui.includes('scheduleRenderObservability')&&ui.includes('if(observer)observer.disconnect()')&&ui.includes('observeSystemRoot()')&&ui.includes('if(existing.outerHTML!==html)existing.outerHTML=html')],['observer callback is coalesced rather than directly rerendering',ui.includes('const observer=new MutationObserver(()=>{scheduleRenderObservability();})')&&!ui.includes('queueMicrotask(renderObservability)')],['no new serverless route for shadow observability',!fs.existsSync('api/system/recommendation-shadow.js')],['SYSTEM backend exposes materiality, immutable 4.2 shadow and active 4.3 state',systemApi.includes('readRecentMaterialityAssessments')&&systemApi.includes('readRecentRecommendationShadows')&&systemApi.includes('readIntelligenceCurrent')&&systemApi.includes('activeRecommendationWrite:true')&&systemApi.includes('todayActivation:true')],['SYSTEM exposes secret-safe release environment probes',systemApi.includes('releaseEnvironment:{databaseConfigured:databaseConfigured()')&&systemApi.includes('secretsExposed:false')],['materiality engine remains 4.1',materiality.includes("MATERIALITY_ENGINE_VERSION = '4.1.0'")],['recommendation engine remains isolated 4.2 shadow audit',shadow.includes("RECOMMENDATION_ENGINE_VERSION = '4.2.0-shadow.1'")&&shadow.includes("RECOMMENDATION_ENGINE_MODE = 'SHADOW'")&&shadow.includes('mayNotAlterToday:true')],['all four materiality states remain visible',['RECORD_ONLY','UPDATE_STATE','RECOMPUTE_RECOMMENDATION','SAFETY_OVERRIDE'].every(x=>materiality.includes(x))]];let bad=0;for(const [name,ok] of checks){console.log(ok?'PASS':'FAIL',name);if(!ok)bad++;}if(bad)process.exit(1);console.log('PASS Tranche 4.1 materiality + 4.2 shadow audit + 4.3 active/system observability contract');
