const FZ_SYSTEM_INTELLIGENCE={system:null,training:null,trends:null,originalFetch:window.fetch.bind(window),mountedRoot:null,observer:null,observerConnected:false,renderScheduled:false,rendering:false};
function canonicalRoute(input){const raw=typeof input==='string'?input:input?.url;if(!raw)return null;try{const url=new URL(raw,window.location.origin);if(url.origin!==window.location.origin)return null;if(url.pathname==='/api/system/status')return 'system';if(url.pathname==='/api/training/memory')return 'training';if(url.pathname==='/api/trends/current')return 'trends';return null;}catch{return null;}}
function scheduleRenderObservability(){if(FZ_SYSTEM_INTELLIGENCE.renderScheduled)return;FZ_SYSTEM_INTELLIGENCE.renderScheduled=true;queueMicrotask(()=>{FZ_SYSTEM_INTELLIGENCE.renderScheduled=false;renderObservability();});}
function captureCanonicalResponse(response,route){if(!response?.ok||!route)return;response.clone().json().then(payload=>{if(!payload?.ok)return;FZ_SYSTEM_INTELLIGENCE[route]=payload;scheduleRenderObservability();}).catch(()=>{});}
window.fetch=async function fzSystemIntelligenceFetch(input,init){const route=canonicalRoute(input);const response=await FZ_SYSTEM_INTELLIGENCE.originalFetch(input,init);if(route)captureCanonicalResponse(response,route);return response;};
function fmtTime(value){if(!value)return '—';const d=new Date(value);if(Number.isNaN(d.getTime()))return '—';return new Intl.DateTimeFormat('en-ZA',{timeZone:'Africa/Johannesburg',day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit',hour12:false}).format(d);}
function words(value){return String(value||'—').replaceAll('_',' ').replace(/\b\w/g,c=>c.toUpperCase());}
function tone(level){if(level==='SAFETY_OVERRIDE'||level==='ABSORB'||level==='WITHHELD')return 'bad';if(level==='RECOMPUTE_RECOMMENDATION'||level==='MAINTAIN')return 'warn';return 'good';}
function materialitySection(payload){const materiality=payload?.intelligence?.materiality;if(!materiality)return null;const latest=Array.isArray(materiality.assessments)?materiality.assessments[0]:null;const m=latest?.materiality||null;const reasons=Array.isArray(m?.reasonCodes)?m.reasonCodes.slice(0,3).map(words).join(' · '):'No materiality assessment persisted yet.';const domains=Array.isArray(m?.affectedDomains)?m.affectedDomains.slice(0,4).map(words).join(' · '):'—';const summary=latest?.evidenceSummary||'The engine is active and waiting for new canonical evidence.';return `<div class="section" data-materiality-observability data-dynamic-source="intelligence"><div class="section-head"><h2>Adaptive Intelligence</h2><p>Tranche 4.1 materiality · visible and auditable · material evidence can trigger the recommendation pipeline</p></div><div class="status-grid"><div class="status-card"><small>Materiality engine</small><b class="good">v${materiality.engineVersion||'—'}</b><p>${materiality.count||0} recent persisted assessment(s) in this view.</p></div><div class="status-card"><small>Latest materiality</small><b class="${tone(m?.level)}">${words(m?.level||'NO ASSESSMENT')}</b><p>${latest?`Assessed ${fmtTime(latest.assessedAt)}`:'No assessment persisted yet.'}</p></div><div class="status-card"><small>State update</small><b class="${m?.shouldUpdateState?'warn':'good'}">${m?.shouldUpdateState?'REQUESTED':'NO CHANGE'}</b><p>${domains}</p></div><div class="status-card"><small>Recommendation trigger</small><b class="${m?.blocksExistingRecommendation?'bad':m?.shouldRecomputeRecommendation?'warn':'good'}">${m?.blocksExistingRecommendation?'BLOCKED':m?.shouldRecomputeRecommendation?'RECOMPUTE':'UNCHANGED'}</b><p>4.2 preserves the audit decision; 4.3 may project it only after canonical convergence.</p></div></div><div class="card rich"><div class="eyebrow">LATEST EVIDENCE ASSESSMENT</div><h3>${words(latest?.sourceType||'Waiting for evidence')}</h3><p>${summary}</p><div class="microline"><strong>Why:</strong> ${reasons}</div></div></div>`;}
function recommendationShadowSection(payload){const shadow=payload?.intelligence?.recommendationShadow;if(!shadow)return null;const current=payload?.intelligence?.current||null;const active=current?.activeRecommendation||null;const latest=Array.isArray(shadow.evaluations)?shadow.evaluations[0]:null;const objective=latest?.contextSummary?.primaryObjective;const gap=latest?.contextSummary?.topMeasurementGaps?.[0];const why=latest?.explanation?.athleteFacing?.whyNow||latest?.explanation?.recommendation?.whyThisLane||'No shadow recommendation has been persisted yet.';const activeMatches=Boolean(active&&latest?.recommendationId&&active.shadowRecommendationId===latest.recommendationId&&active.contextFingerprint&&current?.dependencyState?.activeMatchesShadow!==false);const activeLabel=active?.status==='WITHHELD'?'WITHHELD':active?.fzRecommendedLane||active?.status||'WAITING';const activeText=activeMatches?'4.3 projection matches the latest immutable shadow.':active?'Active projection is stale or pending convergence.':'No active 4.3 projection has been persisted yet.';return `<div class="section" data-recommendation-shadow data-dynamic-source="recommendation-shadow"><div class="section-head"><h2>Recommendation Audit</h2><p>Tranche 4.2 immutable decision trace → Tranche 4.3 controlled athlete-facing projection</p></div><div class="status-grid"><div class="status-card"><small>Shadow engine</small><b class="good">${shadow.engineVersion||'—'}</b><p>${words(shadow.mode||'SHADOW')} · ${shadow.count||0} recent evaluation(s).</p></div><div class="status-card"><small>Latest audited lane</small><b class="${tone(latest?.lane)}">${words(latest?.lane||latest?.status||'WAITING')}</b><p>${latest?`Evaluated ${fmtTime(latest.evaluatedAt)}`:'Waiting for a material recomputation trigger.'}</p></div><div class="status-card"><small>Confidence</small><b class="${latest?.confidence==='LOW'?'warn':'good'}">${words(latest?.confidence||'—')}</b><p>${gap?`Top unknown: ${words(gap.measurementId)}`:'No top measurement gap recorded in this view.'}</p></div><div class="status-card"><small>Active TODAY projection</small><b class="${activeMatches?'good':active?'warn':'good'}">${words(activeLabel)}</b><p>${activeText}</p></div></div><div class="card rich"><div class="eyebrow">LATEST AUDITED DECISION</div><h3>${objective?.name||'No active shadow evaluation yet'}</h3><p>${why}</p><div class="microline"><strong>Trigger:</strong> ${words(latest?.trigger?.materialityLevel||latest?.trigger?.type||'—')} · <strong>Recommendation ID:</strong> ${latest?.recommendationId||'—'}</div></div></div>`;}
function dynamicIntegritySection(){const training=FZ_SYSTEM_INTELLIGENCE.training;const trends=FZ_SYSTEM_INTELLIGENCE.trends;if(!training&&!trends)return null;const integrity=training?.integrity||{},quality=trends?.quality||{},freshness=trends?.freshness||{},intent=trends?.trainingIntent?.counts||{};const pending=Array.isArray(quality.loadMissingDates)?quality.loadMissingDates.length:0,unresolved=Number(integrity.unlinkedSessionFeedback||0),enriched=Number(quality.historicallyEnrichedSessions||0);return `<div class="section" data-dynamic-integrity data-dynamic-source="runtime-integrity"><div class="section-head"><h2>Dynamic Runtime Integrity</h2><p>Live source evidence → canonical persistence → late reconciliation → Trends derivation · no shell deployment</p></div><div class="status-grid"><div class="status-card"><small>Training evidence</small><b class="good">DYNAMIC</b><p>Latest evidence ${fmtTime(freshness.latestTrainingEvidenceAt)} · persisted-first source reconciliation.</p></div><div class="status-card"><small>Athlete Memory links</small><b class="${unresolved?'warn':'good'}">${unresolved?`${unresolved} UNRESOLVED`:'RECONCILED'}</b><p>${Number(integrity.lateLinkedAthleteEvents||0)} late-bound event(s) in the current memory window.</p></div><div class="status-card"><small>NCL evidence</small><b class="${pending?'warn':'good'}">${pending?`${pending} PENDING`:'COMPLETE'}</b><p>${words(quality.evidencePolicy||'MONOTONIC BEST AVAILABLE')} · ${enriched} session(s) protected from sparse-source regression.</p></div><div class="status-card"><small>Inferred intent</small><b class="good">A ${Number(intent.ABSORB||0)} · M ${Number(intent.MAINTAIN||0)} · D ${Number(intent.ADAPT||0)}</b><p>Historical lanes remain descriptive evidence; TODAY follows only the current canonical 4.3 projection.</p></div></div></div>`;}
function placeSection(attribute,html,afterAttribute=null){const root=document.getElementById('system');if(!root||!html)return false;const existing=root.querySelector(`[${attribute}]`);if(existing){if(existing.outerHTML!==html)existing.outerHTML=html;return true;}const after=afterAttribute?root.querySelector(`[${afterAttribute}]`):null;if(after)after.insertAdjacentHTML('afterend',html);else{const first=root.querySelector(':scope > .section');if(first)first.insertAdjacentHTML('afterend',html);else root.insertAdjacentHTML('beforeend',html);}return true;}
function observeSystemRoot(){const root=document.getElementById('system'),observer=FZ_SYSTEM_INTELLIGENCE.observer;if(!root||!observer)return;observer.disconnect();observer.observe(root,{childList:true,subtree:false});FZ_SYSTEM_INTELLIGENCE.observerConnected=true;}
function renderObservability(){if(FZ_SYSTEM_INTELLIGENCE.rendering)return;FZ_SYSTEM_INTELLIGENCE.rendering=true;const observer=FZ_SYSTEM_INTELLIGENCE.observer;if(observer)observer.disconnect();try{placeSection('data-materiality-observability',materialitySection(FZ_SYSTEM_INTELLIGENCE.system));placeSection('data-recommendation-shadow',recommendationShadowSection(FZ_SYSTEM_INTELLIGENCE.system),'data-materiality-observability');placeSection('data-dynamic-integrity',dynamicIntegritySection(),'data-recommendation-shadow');FZ_SYSTEM_INTELLIGENCE.mountedRoot=document.getElementById('system');}finally{FZ_SYSTEM_INTELLIGENCE.rendering=false;observeSystemRoot();}}
const observer=new MutationObserver(()=>{scheduleRenderObservability();});FZ_SYSTEM_INTELLIGENCE.observer=observer;function startSystemIntelligence(){observeSystemRoot();}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',startSystemIntelligence,{once:true});else startSystemIntelligence();
