{
  "name": "fz-performance",
  "version": "0.7.0-consolidation",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs && node scripts/clean-shell.mjs && node scripts/clean-smoke.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "@neondatabase/serverless": "1.1.0"
  }
}
