# v0.5.0 Release Gate

This file is a temporary checkpoint marker. Final main must contain the deterministic build pipeline, immutable release payload, same-origin runtime-state gateway, release manifest, and passing smoke checks before production promotion.
