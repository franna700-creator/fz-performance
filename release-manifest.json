{
  "release": "0.7.0-rc.1",
  "releaseDate": "2026-09-10",
  "classification": "PRODUCT RELEASE — DYNAMIC RUNTIME / LIVE SOURCE ORCHESTRATION / TRANCHE 4.1 OBSERVABILITY / DEPLOYMENT HARDENING",
  "sourceModel": "Neon operational truth with explicit five-minute source refresh for physiology and training; canonical runtime reread after persistence; immutable Vercel state transport retained as fail-stale fallback only",
  "features": [
    "explicit Garmin physiology source refresh on load, every five minutes while visible, and wake-up checks",
    "persisted-first Live Physiology with four scrubbed intraday traces",
    "persisted-first training memory with five-minute source reconciliation and manual Sync workouts",
    "immediate canonical reread after source persistence so downstream Trends/System/Training views move without shell deployment",
    "read-only Tranche 4.1 materiality observability in SYSTEM",
    "production smoke uses dynamic invariants instead of frozen rolling NCL totals",
    "automatic Vercel Git deployments disabled; deliberate pinned release only",
    "Neon-primary runtime/state documentation aligned with current production architecture"
  ],
  "dynamicRuntime": {
    "canonicalRereadMs": 300000,
    "physiologySourceCheckMs": 300000,
    "trainingSourceCheckMs": 300000,
    "wakeThrottleMs": 120000,
    "manualGarminRefresh": true,
    "manualWorkoutSync": true,
    "requiresShellDeploymentForDataRefresh": false
  },
  "intelligenceBoundary": {
    "materialityEngine": "4.1.0",
    "observability": true,
    "automaticRecommendationRecompute": false,
    "nextTranche": "4.2"
  },
  "releaseGate": {
    "deterministicBuild": "required",
    "staticChecks": "required",
    "schemaChecks": "required",
    "dynamicRuntimeChecks": "required",
    "materialityChecks": "required",
    "preview": "one deliberate pinned preview",
    "browserSmoke": "required desktop + mobile",
    "productionPromotion": "exact-tested-candidate only",
    "productionReadback": "required",
    "rollback": "required"
  }
}
